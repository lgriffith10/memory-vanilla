import * as bootstrap from 'bootstrap'
import _ from 'lodash'

import Game from './classes/Game';

import './scss/style.scss';


document.addEventListener('DOMContentLoaded', () => {
    const gameContainer = new Game();
});